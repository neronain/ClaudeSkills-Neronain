# Install claude-code

Copy this `dgx-spark-model-deployer` directory to the platform's skill root.

See `references/platform-notes.md` and the team-pack README for the default path.

Validate:

```bash
python3 scripts/validate_skill.py .
```
